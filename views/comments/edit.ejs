<!DOCTYPE html>
<html lang="en">
<% include ../partials/head %>
<body>
<% include ../partials/nav %>

<div class="container padTop">
  <div class="container hide-small padTop">
    <div class="row">
      <h1 class="new">Edit Comment</h1>

      <div class="form-horizontal">
          <form action="/campgrounds/<%= campgroundId %>/comments/<%= comment._id %>?_method=PUT" method="POST">
            <!--<div class="form-group">
              <label for="author" class="control-label col-sm-2">Author</label>
              <div class="col-sm-10">
                <input id="author" class="form-control" type="text" name="comment[author]" value="<%= comment.name %>">
              </div>
            </div>-->
            <div class="form-group">
              <label for="comment" class="control-label col-sm-2">Comment</label>
              <div class="col-sm-10">
                <textarea name="comment[text]" id="comment" class="form-control" rows="6"><%= comment.text %></textarea>
              </div>
            </div>
            <div class="text-right">
              <button class="btn btn-primary btn-lg padBot padTop">Add Comment</button>
            </div>
          </form>
      </div>
    </div>
  </div>
</div>
<div class="navbar-fixed-bottom">
  <% include ../partials/footer %>
</div>
</body>
</html>
